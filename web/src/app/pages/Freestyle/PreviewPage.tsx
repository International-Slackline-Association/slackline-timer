import { FreestyleTimerDisplay } from './FreestyleTimerDisplay';

// Projector preview (/freestyle/preview): defaults to the magenta chroma-key
// ground — variant/bg rules in FreestyleTimerDisplay.
export const FreestylePreviewPage = () => <FreestyleTimerDisplay variant="projector" />;
