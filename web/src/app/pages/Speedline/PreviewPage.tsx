import { SpeedlineTimerDisplay } from './SpeedlineTimerDisplay';

// Projector preview (/speedline/preview): defaults to the magenta chroma-key
// ground — variant/bg rules in SpeedlineTimerDisplay, where all logic lives.
export const SpeedlinePreviewPage = () => <SpeedlineTimerDisplay variant="projector" />;
